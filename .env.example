# Radar Docker Environment Configuration
# Copy to .env and customize as needed
#
# Data Storage:
# - Conversations, memories, and personalities are stored in the radar-data volume
# - This data persists across container restarts and rebuilds
# - To backup: docker run --rm -v radar-data:/data -v $(pwd):/backup alpine tar czf /backup/radar-backup.tar.gz /data
# - To restore: docker run --rm -v radar-data:/data -v $(pwd):/backup alpine tar xzf /backup/radar-backup.tar.gz -C /

# =============================================================================
# LLM Configuration
# =============================================================================

# Provider: "ollama" (default) or "openai" (for OpenAI-compatible APIs)
# RADAR_LLM_PROVIDER=ollama

# LLM API endpoint
# Default: http://host.docker.internal:11434 (Ollama on host machine)
# For remote Ollama: http://192.168.1.50:11434
# For OpenAI: https://api.openai.com/v1
# For LiteLLM proxy: http://litellm.internal:4000
# RADAR_LLM_BASE_URL=http://host.docker.internal:11434

# Model name (provider-specific)
# Ollama: qwen3:latest, llama3.2, etc.
# OpenAI: gpt-4o, gpt-4o-mini, etc.
# RADAR_LLM_MODEL=qwen3:latest

# API key (required for OpenAI-compatible providers)
# RADAR_API_KEY=sk-...

# =============================================================================
# Embedding Configuration
# =============================================================================

# Provider: "ollama" (default), "openai", "local", or "none"
# Use "local" if you built with LOCAL_EMBEDDINGS=true
# Use "none" to disable semantic memory
# RADAR_EMBEDDING_PROVIDER=ollama

# Embedding model name
# Ollama: nomic-embed-text
# OpenAI: text-embedding-3-small
# Local: all-MiniLM-L6-v2
# RADAR_EMBEDDING_MODEL=nomic-embed-text

# Embedding API endpoint (defaults to LLM URL if not set)
# RADAR_EMBEDDING_BASE_URL=

# Embedding API key (defaults to LLM key if not set)
# RADAR_EMBEDDING_API_KEY=

# =============================================================================
# Web UI Configuration
# =============================================================================

# Authentication token (recommended when exposing to network)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# RADAR_WEB_AUTH_TOKEN=

# =============================================================================
# Notifications (optional)
# =============================================================================

# ntfy.sh server URL
# RADAR_NTFY_URL=https://ntfy.sh

# ntfy topic name
# RADAR_NTFY_TOPIC=radar-notifications

# =============================================================================
# Personality (optional)
# =============================================================================

# Active personality name or path
# RADAR_PERSONALITY=default
